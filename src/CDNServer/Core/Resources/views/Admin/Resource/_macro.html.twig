{% macro resource_status(resource) %}
    {% if resource.refresh %}
        Marked for refresh <span class="glyphicon glyphicon-remove text-danger"></span>
    {% else %}
        Available <span class="glyphicon glyphicon-ok text-success"></span>
    {% endif %}
{% endmacro %}