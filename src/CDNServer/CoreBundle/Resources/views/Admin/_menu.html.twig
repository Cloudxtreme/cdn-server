<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-admin-collapse">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="{{ path('admin_hub') }}">CDN Admin</a>
		</div>
	
		<div class="collapse navbar-collapse navbar-admin-collapse">
			<ul class="nav navbar-nav">
				{% if app.user %}
				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown">
						<span class="glyphicon glyphicon-th-list"></span> List <b class="caret"></b>
					</a>
					<ul class="dropdown-menu">
                        <li class="{% if active_page == 'list_projects' %}active"{% endif %}"><a href="{{ path('admin_project') }}">View projects</a></li>
						{# <li class="{% if active_page == 'list_apps' %}active"{% endif %}"><a href="{{ path('admin_application_list') }}">View apps</a></li> #}
					</ul>
				</li>
				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown">
						<span class="glyphicon glyphicon-cog"></span> Others <b class="caret"></b>
					</a>
					<ul class="dropdown-menu">
                        {% if is_granted('ROLE_ADMIN') %}
                        <li class="{% if active_page == 'list_usergroups' %}active"{% endif %}"><a href="{{ path('admin_usergroup') }}">View user groups</a></li>
                        <li class="{% if active_page == 'list_users' %}active"{% endif %}"><a href="{{ path('admin_user') }}">View users</a></li>
                        {% endif %}
						<li role="presentation" class="divider"></li>
						<li class="disabled"><a href="">My account..</a></li>
					</ul>
				</li>
				{% endif %}
			</ul>
			
			{% if app.user %}
			<ul class="nav navbar-nav navbar-right">
				<li>
					<a href="{{ path('fos_user_security_logout') }}"><span class="glyphicon glyphicon-log-out"></span> Logout</a>
				</li>
				<li>&nbsp;</li>
			</ul>
			{% endif %}
		</div>
	</div>
</nav>
